spring:
  application:
    name: payment-service
  profiles:
    active: development
  config:
    import: "configserver:${CONFIG_SERVER_URL:http://localhost:8888}"
  cloud:
    config:
      fail-fast: true
