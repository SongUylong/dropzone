# Dropzone Virtual Host Ingress Routing Configuration

# 1. API Gateway Proxy (api.dropzone.local)
server {
    listen 80;
    server_name api.dropzone.local;

    location / {
        proxy_pass http://api-gateway:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /health {
        return 200 'OK';
        add_header Content-Type text/plain;
    }
}

# 2. Keycloak Authentication IAM Proxy (auth.dropzone.local)
server {
    listen 80;
    server_name auth.dropzone.local;

    location / {
        proxy_pass http://192.168.56.13:8089;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# 3. Eureka Service Discovery Dashboard Proxy (eureka.dropzone.local)
server {
    listen 80;
    server_name eureka.dropzone.local;

    location / {
        proxy_pass http://192.168.56.13:8761;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# 4. RabbitMQ Management Dashboard Proxy (rabbit.dropzone.local)
server {
    listen 80;
    server_name rabbit.dropzone.local;

    location / {
        proxy_pass http://192.168.56.13:15672;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# 5. Fallback Default Ingress
server {
    listen 80 default_server;
    server_name _;

    location / {
        proxy_pass http://api-gateway:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
